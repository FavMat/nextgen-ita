# NextGen Ita and CiaoMirta

Project context lives in **[AGENTS.md](AGENTS.md)**, next to this file. It is
the shared source of truth for every coding agent, so there is one description
to keep accurate rather than two that drift apart.

Workspace wide working rules are in `../AGENTS.md`.

Read both before making changes.

---

*This file used to hold its own copy of the project description. It went stale:
it described a white theme when the site is dark, the wrong fonts, and three
pages that had already been deleted. That is exactly the failure mode a single
shared file avoids.*
